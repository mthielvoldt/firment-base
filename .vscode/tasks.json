{
  "tasks": [
    {
      "label": "build-tests",
      "type": "shell",
      "command": "cmake --build build --target fmt_base_test",
      "options": {
        "cwd": "${workspaceFolder}/test"
      },
    }
  ],
  "version": "2.0.0"
}